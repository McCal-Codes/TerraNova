[package]
name = "bridge-save"
version = "0.1.0"
description = "Shared Hytale save reader for TerraNova Bridge (player/world resolution)"
edition = "2021"
rust-version = "1.77"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

#!/bin/sh
# McCal-Codes-only commits: strip Co-authored-by trailers before finalize.

msg_file=$1
[ -f "$msg_file" ] || exit 0

sed -i.bak -E "/^[Cc]o-[Aa]uthored-[Bb]y:/d" "$msg_file"
rm -f "$msg_file.bak"

exit 0
